#pragma once

#include <cstddef>

namespace yuengine::diagnostics
{
inline constexpr std::size_t MAX_DIAGNOSTICS_EVENTS = 16U;
inline constexpr std::size_t MAX_DIAGNOSTICS_COUNTERS = 8U;
inline constexpr std::size_t MAX_DIAGNOSTICS_EVENT_IDS = 8U;
inline constexpr std::size_t MAX_DIAGNOSTICS_COUNTER_IDS = 8U;
}
