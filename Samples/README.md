# Samples

本目录保存贴近引擎的示例和 smoke demo。示例可以拥有独立构建文件和资源，但它们不是 `Src/YuEngine` 运行时代码，也不能单独作为某个运行时子系统已经完整落地的证明。

每个示例自己的生成目录、截图、日志、复制出来的 DLL 和本地 SDK 输出都应放在对应示例目录下的 ignore 范围内。

第三方源码或二进制不能藏在示例构建输出里。示例需要在 README 中写清依赖来源；只有许可证和来源明确、且确实需要随仓库分发的包，才放入 `ThirdParty`。
